# Integration regression fixtures

This directory is the release-gated home for end-to-end CLI/manifest regressions.
Unlike `examples/`, its contents are test fixtures rather than user-facing tutorials.

Small wrapper manifests may include an example while that scenario is still useful in
both roles. New regression-only scenarios should live here directly.
